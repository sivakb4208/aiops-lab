---
- name: AlertManager Webhook Rulebook

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Test Webhook
      condition: event.payload is defined
      action:
        run_job_template:
          name: AIOps Test Job
